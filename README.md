# Matching Engine: Go vs Rust

Движок матчинга лимитных ордеров на Go и на Rust с одинаковой семантикой,
одинаковым потоком ордеров и одинаковым протоколом замера.

![Протокол автора, 5 раундов: Go 37.8 M ордеров/с против Rust 103.5 M — x2.7](benchmark-5-rounds.svg)

Замер по протоколу автора (`WARM=1 ROUNDS=5 ./run_benchmark.sh`, Apple M4):
в установившемся режиме Go даёт 2.65 ms на 100 000 ордеров (37.8 M ордеров/с),
Rust — 0.97 ms (103.5 M ордеров/с), **Rust быстрее в 2.7 раза**; сделки и объём
у обеих сторон совпадают (77 576 / 1 973 216). Код Go не изменён.

## Десять реализаций, один движок

Тот же генератор ордеров (Xorshift64, seed `0xDEADBEEFCAFE1234`), та же
семантика матчинга (price-time priority, FIFO внутри уровня, частичные
исполнения), тот же протокол замера и тот же формат вывода — на десяти
языках. Каждая реализация обязана дать `trades=77576, volume=1973216,
resting=21620, 55/50 уровней` и совпасть с Rust на других seed; иначе
прогон помечается FAIL. Внешних зависимостей нет ни у одной.

![Все реализации: M ордеров/с по протоколу автора](benchmark-all-languages.svg)

| # | Язык | ms на 100 000 ордеров | M ордеров/с | к лучшему | к Go | Структура |
|---|---|---|---|---|---|---|
| 1 | **ASM** (ARM64, ядро на ассемблере + C-обвязка) | **0.83** | **120.8** | x1.00 | **x3.2** | плоская книга + битовая карта, регистры |
| 2 | C | 0.98 | 102.2 | x1.18 | x2.7 | плоская книга + битовая карта |
| 3 | Rust | 0.98 | 101.7 | x1.19 | x2.7 | плоская книга + битовая карта |
| 4 | C++ | 1.02 | 97.9 | x1.23 | x2.6 | плоская книга + битовая карта |
| 5 | C# (.NET 10) | 1.21 | 82.4 | x1.46 | x2.2 | плоская книга, массивы/Span |
| 6 | NodeJS (v25) | 1.99 | 50.3 | x2.40 | x1.3 | типизированные массивы |
| 7 | Java (JDK 26) | 2.22 | 45.1 | x2.68 | x1.2 | примитивные массивы |
| 8 | **Go, код автора (не изменён)** | 2.65 | 37.7 | x3.20 | x1.0 | сортированный массив уровней, O(n)-сдвиги |
| 9 | PHP 8.5 | 5.05 | 19.8 | x6.1 | x0.5 | плоские массивы |
| 10 | Python 3.14 | 22.14 | 4.5 | x26.7 | x0.1 | списки |

Apple M4, `WARM=1 ROUNDS=7 ./run_benchmark.sh`, медиана Average по 7 раундам;
Go — единственный участник на исходной структуре данных автора, остальные
девять реализуют одну и ту же плоскую книгу с битовой картой (порт
`rust/src/orderbook.rs`), поэтому разница между ними — это разница языков и
рантаймов при одном алгоритме. Rust, C и C++ — один компиляторный бэкенд
(LLVM) и один алгоритм, поэтому они в пределах шума друг от друга; ассемблер
выигрывает ~15% ручным распределением регистров.

## Запустить у себя

Нужны Go 1.21+ и Rust 1.85+; для остальных — clang (Xcode CLT), .NET 10,
JDK, PHP 8, Node — если чего-то нет, эта реализация пропускается.

Все реализации, протокол автора, три холостых прогона и 7 раундов на
каждую, сводная таблица с местами в конце:

```bash
WARM=1 ROUNDS=7 ./run_benchmark.sh
```

Только Go и Rust, один прогон — ровно как в оригинале:

```bash
LANGS="go rust" ./run_benchmark.sh
```

Любая реализация отдельно (`--iters N`, `--seed HEX`):

```bash
./c/run.sh --iters 21
```

Расширенный замер Rust — 101 итерация, публикуются min / median / p95 / p99:

```bash
cd rust && cargo run --release --bin matching-engine-rust -- --iters 101
```

Тесты и линтер:

```bash
cd rust && cargo test --release && cargo clippy --all-targets -- -D warnings
```

Перед замером — `uptime`: при `load average` выше ~4 числа плавают. Первый
запуск процесса после простоя на Mac идёт вдвое медленнее, а 5 итераций
протокола автора целиком в него попадают — поэтому `WARM=1` делает три
холостых прогона, а `ROUNDS` показывает разброс вместо одного случайного
числа.
